/**
 * Login endpoint tests — placeholder.
 *
 * Test cases to implement:
 *   - Successful login with verified user
 *   - 401 for wrong password
 *   - 401 for non-existent email
 *   - 403 for unverified email
 *   - 403 for locked account
 *   - Account locks after 5 failed attempts
 *   - Account unlocks after 15 minutes
 *   - Login resets failed attempt counter
 *   - Rate limiting (5 req / 15 min)
 *   - Sets HttpOnly refresh token cookie
 *   - Returns access token in response body
 *   - Creates LOGIN audit log on success
 *   - Creates LOGIN_FAILED audit log on failure
 *   - 422 for missing email field
 *   - 422 for invalid email format
 *   - 422 for missing password field
 */

console.log('Login tests placeholder — install jest/mocha to run tests.');
